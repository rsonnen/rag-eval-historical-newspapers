[project]
name = "historical-newspapers-scripts"
version = "0.1.0"
description = "Scripts to download historical newspapers from Chronicling America for document-processor testing"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "tqdm>=4.66.0",
]

[dependency-groups]
dev = []
